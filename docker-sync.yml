version: '2'

options:
  verbose: false

syncs:
  rails_seed-sync:
    src: ./
    sync_strategy: native_osx
    notify_terminal: true
    sync_excludes:
      - .bundle
      - .git
      - .gitignore
      - .DS_Store
      - log
      - node_modules
      - tmp
      - vendor/bundle
